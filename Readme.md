# Todo App made for Windows

Dependencies:

* __make__
* __VcXsrvs__ X Server

## Compile and Run

```sh
make build
make run
```

```sh
# once inside the container, /app
./compile.sh
```

## Run

```sh
make run
./compile.sh
```
